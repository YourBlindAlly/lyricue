/**
 * The Dropbox subfolder songwriting works-in-progress live in. Kept
 * entirely separate from the main song library — a notebook song is only
 * browsed from here (the Library screen's Notebook filter), never added
 * to Library/Search/setlists on its own, until it's explicitly promoted.
 */
export const NOTEBOOK_FOLDER = '/notebook';
